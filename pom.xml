<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <!-- 继承:父级项目坐标 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.6.2</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <!-- GVA -->
    <groupId>com.yunqi</groupId>
    <artifactId>SuBoot</artifactId>
    <version>1.0.3-SNAPSHOT</version>

    <!-- 项目名称 -->
    <name>SuBoot</name>
    <packaging>pom</packaging>

    <modules>
        <module>su-system</module>
        <module>su-admin</module>
    </modules>

    <!-- ======================================== -->
    <!-- 统一管理全局变量 -->
    <!-- ======================================== -->
    <properties>
        <!-- 修订版本 -->
        <revision>1.0.6-SNAPSHOT</revision>
        <java.version>1.8</java.version>

        <!-- utf-8 -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <!-- ======================================== -->
    <!-- 公共依赖 -->
    <!-- ======================================== -->
    <dependencyManagement>
        <dependencies>
            <!-- aop -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-aop</artifactId>
                <version>2.6.2</version>
            </dependency>
            <!-- validation -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-validation</artifactId>
                <version>2.6.2</version>
            </dependency>
            <!-- lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>1.18.22</version>
            </dependency>
            <!-- jasypt -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-jasypt</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 公共组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-common</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 数据库组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-database</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 定时任务组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-quartz</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 权限组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-security</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 邮件组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-mail</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 日志组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-log</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- web组件 -->
            <dependency>
                <groupId>com.yunqi.starter</groupId>
                <artifactId>su-starter-web</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- ======================================== -->
            <!-- 模块依赖 -->
            <!-- ======================================== -->
            <dependency>
                <groupId>com.yunqi</groupId>
                <artifactId>su-system</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>

    </dependencyManagement>

    <!-- maven仓库 -->
    <repositories>

        <!-- maven.apache.org 中央仓库 -->
        <repository>
            <id>central-repos</id>
            <name>Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <!-- nutz 中央仓库 -->
        <repository>
            <id>nutz-snapshots</id>
            <name>nutz Releases</name>
            <url>https://jfrog.nutz.cn/artifactory/snapshots</url>
            <snapshots>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

</project>
